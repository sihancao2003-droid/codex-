# 上游功能与可能的 Token 消耗

本文把原作者鲸鱼挂件的功能、Codex/ZCode 上游数据源，以及可能产生的模型用量分开说明。

## 上游功能清单

原作者项目当前的功能描述包括：余额、今日用量、峰谷计价、随机台词、音效、每轮消耗、自定义形象/动图/音效、吸边与翻转等。见[原作者项目的 package.json](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget/blob/main/package.json)。本移植版的对应关系如下：

| 上游能力 | 本项目实现 | 数据来源或说明 |
| --- | --- | --- |
| 鲸鱼形象、气泡、呼吸/按压动画 | 保留 | 原项目素材与动画设计 |
| 待机隐藏余额、摸头后显示用量 | 保留并调整 | 第一次左键显示 5 小时，第二次点击气泡显示每周 |
| 随机彩蛋文案 | 保留 | 右键菜单可编辑；显示概率可配置 |
| 多套触摸音效、音量 | 保留 | 右键菜单选择小黄鸭、音效 1、随机或关闭 |
| 拖动、吸附、左右翻转 | 保留 | 桌面 Electron 窗口实现 |
| 固定在最高层 | 增加 | 右键菜单可关闭 |
| Codex 任务状态动画 | 增加 | Hooks 事件流：开始、提示、工具、确认、结束、中断 |
| Codex 5 小时/每周剩余用量 | Codex 模式 | `codex app-server` 的 `account/rateLimits/read` |
| ZCode 个人套餐 5 小时/每周用量 | ZCode 模式 | BigModel `monitor/usage/quota/limit`；这是套餐窗口的正确来源 |
| ZCode 模型/试用额度 | 保留为后台数据 | ZCode `billing/balance`；不再冒充 5 小时或每周余额 |
| 今日 ZCode token 用量 | 增加 | 读取本机 `~/.zcode/cli/rollout/model-io-*.jsonl`，按模型汇总 |
| 任务结束提醒 | 保留 | 显示最新剩余百分比；有 token 统计时同时显示本轮/今日数量 |

## 余额查询本身是否消耗 Token

以下操作只读本机事件、日志或套餐接口，不会调用模型，因此直接消耗为 **0 token**：

| 操作 | 直接 token 消耗 |
| --- | ---: |
| 启动挂件、刷新余额 | 0 |
| 左键摸头、查看 5 小时/每周余额 | 0 |
| 右键菜单、编辑彩蛋、切换音效/音量/大小 | 0 |
| 拖动、吸边、固定最上层 | 0 |
| 读取 Codex/ZCode 事件和本地用量日志 | 0 |

这些操作只会展示或统计上游智能体已经产生的消耗；它们不会因为“查询余额”额外扣套餐额度。

## 智能体任务的可能消耗

单个功能没有固定 token 价格。实际消耗取决于输入上下文、缓存命中率、输出长度、工具调用次数、是否反复修复，以及智能体需要多少轮模型调用。下面是工程估算范围，不是账单承诺：

| 任务类型 | 常见总 token 范围 | 备注 |
| --- | ---: | --- |
| 简单问答、单文件小修改 | 2K–20K | 通常 1–5 次模型调用 |
| 小型 Bug 修复、带测试 | 10K–80K | 通常需要读取文件、修改、运行测试 |
| 中型功能开发 | 30K–200K | 可能包含多轮工具调用和回归 |
| 大型重构、跨模块排错 | 100K–1M+ | 取决于仓库大小和失败重试次数 |
| 阅读大型仓库/长日志 | 50K–500K+ | 输入上下文和缓存命中率影响最大 |
| 图片/视觉理解 | 无固定值 | 由图片尺寸、细节和模型实现决定 |
| Web Search、Web Reader、Zread 等 MCP | 无固定 token 范围 | 上游按工具调用计费，并且工具结果还可能进入模型上下文 |

ZCode 官方把一次 prompt 估算为大约 15–20 次模型调用，因此“一个用户问题”不等于一次模型请求。官方也明确说明，真实用量会随任务复杂度、代码库大小和自动接受设置变化。

## ZCode 套餐额度换算

ZCode 套餐不是简单的“固定 token 包”，而是 credits。官方计算式为：

```text
模型 credits =
  (输入 tokens × 输入倍率
   + 缓存命中输入 tokens × 缓存倍率
   + 输出 tokens × 输出倍率) / 10,000
```

当前官方倍率：

| 模型 | 输入 | 缓存命中输入 | 输出 |
| --- | ---: | ---: | ---: |
| GLM-5.3 | 6.9 | 1.7 | 24 |
| GLM-5.3-Flash | 2.3 | 0.56 | 8 |

另外，Web Search、Web Reader、Zread 是按调用计的 MCP 能力，官方页面列出的单次系数为 1.2；模型在非高峰时段按标准 credits 的 50% 计。套餐额度目前为：Lite 5 小时 2,000 / 每周 10,000 credits，Pro 12,000 / 60,000，Max 28,000 / 140,000。

一个便于理解的例子：10K 个未命中缓存的输入 token + 2K 输出 token，按标准时段计算，约为 GLM-5.3 的 117 credits，或 GLM-5.3-Flash 的 39 credits；实际会因缓存命中、工具结果和多轮调用而变化。

官方还给出了按周的 token 估算范围：以 95% 缓存命中率为例，Lite 大约支持 GLM-5.3 的 48–97M token，或 GLM-5.3-Flash 的 146–292M token；这是套餐总量的估算，不是每条消息的固定上限。

参考：[Z.AI Coding Plan 总览](https://docs.z.ai/devpack/overview)、[Z.AI 用量查询插件说明](https://docs.z.ai/devpack/extension/usage-query-plugin)。

## 读取口径

- 挂件的 5 小时与每周百分比来自套餐配额接口，显示的是剩余百分比。
- “今日已用”来自本机 ZCode rollout 日志，是实际日志中记录的 token 汇总。
- 本文中的任务范围是经验估算；需要精确核对时，以 ZCode Usage Statistics 或日志中的实际 input/output/cache 字段为准。

## 作者最新版功能的桌面移植状态

作者当前 `main` 的前端已纳入 `ui/whale-widget.js`，并通过 `upstream-server.cjs` 提供本地同源路由，因此以下功能现在可以在桌面挂件中使用：

- 模块化泡泡、点击序列、A/B 加权随机选择、随机语句和随机图片。
- 文本/链接/余额/今日用量/峰谷模块，以及逐模块字体、字号、粗体、斜体、下划线、纯色和渐变样式。
- 泡泡模块拖拽排序、拆行、合并、模块库和长按排序。
- 角色图库、PNG/GIF 角色导入、角色置顶/删除和图片裁剪。
- 泡泡图库、内置 `petpet`/`money1`、PNG/GIF 导入和删除。
- 预置音效、自定义音效组、音频片段导入/裁剪/试听、任务结束音。
- 用量记录窗口、今日/近 7 天/全部记录、模型占比和搜索。
- 余额预警、今日预算、每轮消耗提示内容、自动关闭时间和图片模块。
- 吸附区域、自由摆放、翻转线、滚动条避让以及菜单按钮隐藏后右键唤出。

宿主差异：原作者的 DSH 版本可以读取任意 DSH 会话事件和第三方厂商凭据。本移植版将核心余额和事件口径限制为 Codex/ZCode，并在本地提供兼容路由；自定义模型面板保存的配置不会自动获得第三方密钥，避免把密钥写入挂件配置。
